
@*//GENERATED CODE BY VISUAL STUDIO
*@
@model IEnumerable<WebApplicationMVC.Models.Projects>

@{
    ViewData["Title"] = "Featured Projects";
}

<h1 >@ViewData["Title"]</h1>

<p>
    <a class = "projectButton" asp-action="Create" >Add new project</a>
</p>


<div class = "projectsText">
    @foreach(var item in Model){
    }
    </div>


    <div class = "projectsText">

@foreach (var item in Model) {

    <hr/>   
            <h2 class = "projectHeader">
                @Html.DisplayFor(modelItem => item.ProjectName)

                <a href = "@Html.DisplayFor(modelItem => item.URL)" target = "_blank" class = "link-Text" > <i class="fa fa-external-link" aria-hidden="true"></i></a>
            </h2>

            
               <h4>Source Code</h4>

            <a href = "@Html.DisplayFor(modelItem => item.URL)" target = "_blank" class = "link-Text" > <i class="fa fa-github fa-3x" aria-hidden="true"></i>
 @*@Html.DisplayFor(modelItem => item.ProjectName) *@ </a>

            <h4 class = "toolsHeader">Tools</h4>
    <ul>
        <li>JavaScript</li>
        <li>React JS</li>
        <li>HTML</li>
         <li>CSS</li>
         <li>Firebase</li>
    </ul>
            <p>
                @Html.DisplayFor(modelItem => item.Description)
            </p>

         
            @*<p>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </p>*@
}
            

 </div> 
 <hr/>
